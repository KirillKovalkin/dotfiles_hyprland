# dotfiles_hyprland

A concise collection of configuration files and a single setup script for Arch Linux using the Hyprland compositor.

These files provide preferred shell and application settings; the included `install.sh` automates package installation and config deployment.
